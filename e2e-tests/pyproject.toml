[project]
name = "hisui-e2e-tests"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "aiohttp",
  "cryptography",
  "httpx",
  "pytest",
  "pytest-timeout",
  "ruff",
  "ty",
]

[tool.pytest.ini_options]
testpaths = ["obsws", "transcribe"]
timeout = 30
